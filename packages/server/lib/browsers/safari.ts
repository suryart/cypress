import Debug from 'debug'
import { Browser } from './types'

const debug = Debug('cypress:server:browsers:safari')

export async function open (browser: Browser, url, options: any = {}) {
  debug('TODO: add Safari launch code here')
}
